# Cloudflare Pages 配置
# 注意：KV 命名空间和环境变量请在 Cloudflare Dashboard 中配置

name = "birthday-manager"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

# Pages 部署不需要在此配置 KV，KV 绑定在 Dashboard 中设置
# 本地开发时使用 wrangler pages dev 命令
